<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

    <!-- <record id="view_sale_area_form" model="ir.ui.view">
        <field name="name">sale.area.form</field>
        <field name="model">sale.area</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="description"/>
                    </group>    
                </sheet>
            </form>
        </field>
    </record> -->

    <record id="view_sale_area_tree" model="ir.ui.view">
        <field name="name">sale.area.tree</field>
        <field name="model">sale.area</field>
        <field name="arch" type="xml">
            <tree editable="top">
                <field name="name"/>
                <field name="description"/>
            </tree>
        </field>
    </record>      

    <record model="ir.actions.act_window" id="action_sale_area_view">
        <field name="name">sale.area.view</field>
        <field name="res_model">sale.area</field>
        <field name="view_mode">tree</field>
    </record>

    <menuitem id="menu_sale_area" name="Sale Area Configuration" parent="crm.crm_menu_config" action="action_sale_area_view" sequence="7"/>

    </data>
</odoo>